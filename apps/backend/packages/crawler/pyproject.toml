[project]
name = "crawler"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "tsukuneA1", email = "tkschr1125@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "libs",
    "httpx>=0.28.1",
]

[tool.uv.sources]
libs = { workspace = true }

[project.scripts]
crawler = "crawler.main:main"

[build-system]
requires = ["uv_build>=0.10.12,<0.11.0"]
build-backend = "uv_build"
